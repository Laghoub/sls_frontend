export type StudentAttendanceStatus = 'PRESENT' | 'ABSENT' | 'RETARD';
export interface ExpectedStudentAttendance { sessionId:number|null; scheduleEntryId:number; date:string; timeSlotId:number; timeSlotCode:string; startTime:string; endTime:string; classGroupId:number; classGroupName:string; subjectId:number; subjectName:string; teacherId:number; teacherName:string; studentId:number; studentNumber:string; studentName:string; attendanceId:number|null; status:StudentAttendanceStatus|null; lateMinutes:number|null; reason:string|null; justified:boolean; notes:string|null; }
export interface StudentAttendanceSaveRequest { studentId:number; status:StudentAttendanceStatus; lateMinutes:number|null; reason:string|null; justified:boolean; notes:string|null; }
export interface StudentAttendanceBatchRequest { scheduleEntryId:number; date:string; attendances:StudentAttendanceSaveRequest[]; }
export interface StudentAttendance { id:number; classSessionId:number; date:string; timeSlotId:number; timeSlotCode:string; classGroupId:number; classGroupName:string; subjectId:number; subjectName:string; studentId:number; studentNumber:string; studentName:string; status:StudentAttendanceStatus; lateMinutes:number; reason:string|null; justified:boolean; notes:string|null; recordedAt:string|null; validatedAt:string|null; }
export interface StudentAttendanceSummary { total:number; present:number; absent:number; late:number; justifiedAbsences:number; lateMinutes:number; }
export interface StudentAttendanceFilters { from?:string|null; to?:string|null; studentId?:number|null; classGroupId?:number|null; status?:StudentAttendanceStatus|null; justified?:boolean|null; page?:number; size?:number; }
export interface StudentPageResponse<T>{content:T[];page:number;size:number;totalElements:number;totalPages:number;first:boolean;last:boolean;}
